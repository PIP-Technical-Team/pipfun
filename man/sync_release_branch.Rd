% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/branch_management.R
\name{sync_release_branch}
\alias{sync_release_branch}
\title{Create or Update release branch of a GH repo}
\usage{
sync_release_branch(
  owner = getOption("pipfun.ghowner"),
  repo,
  ref_branch = "DEV",
  target_branch = NULL,
  verbose = FALSE
)
}
\arguments{
\item{owner}{Character. The GitHub owner or organization name. Defaults to \code{getOption("pipfun.ghowner")}}

\item{repo}{Character. The name of the repository.}

\item{ref_branch}{Character. The branch from which the release branch should be created or updated. Defaults to \code{"DEV"}}

\item{target_branch}{Character. The name of the release branch to create or update. If \code{NULL}, it will be set to \code{paste0(release, "_", identity)}}

\item{verbose}{logical: whether to print messages about the process. Default is FALSE}
}
\value{
Invisible \code{TRUE} if the process succeeds, otherwise an error message is displayed
}
\description{
This function checks if a GitHub repository has a release branch. If a release branch exists, it updates it with the latest \code{DEV} branch.
If no release branch exists, it creates a new one from \code{DEV}
}
\examples{
\dontrun{
sync_release_branch(owner = "PIP-Technical-Team", repo = "aux_gdp")
}
}
