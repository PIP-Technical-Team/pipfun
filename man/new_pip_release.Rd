% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release_management.R
\name{new_pip_release}
\alias{new_pip_release}
\alias{remove_pip_release}
\title{Create new release for PIP update}
\usage{
new_pip_release(
  release = format(Sys.Date(), "\%Y\%m\%d"),
  identity = getOption("pipfun.identities"),
  verbose = getOption("pipfun.verbose"),
  root_dir = Sys.getenv("PIP_ROOT_DIR"),
  working_dir = fs::path(root_dir, getOption("pipfun.working_dir")),
  ppps = getOption("pipfun.ppps"),
  ...
)

remove_pip_release(
  release,
  identity = getOption("pipfun.identities"),
  verbose = getOption("pipfun.verbose"),
  working_dir = NULL,
  ppps = getOption("pipfun.ppps"),
  confirm_remove = getOption("pipfun.confirm_remove"),
  ...
)
}
\arguments{
\item{release}{character: release name in the form of "\%Y\%m\%d". Defaults to current date.}

\item{identity}{character. Defaults to the first value of \code{getOption("pipfun.identities")}}

\item{verbose}{A logical: whether to print detailed messages
about the process. The default is \code{TRUE}}

\item{root_dir}{character: Root directory for PIP data, defaults to \code{Sys.getenv("PIP_ROOT_DIR")}}

\item{working_dir}{character: Working directory where files will be created. Defaults to a subdirectory of \code{root_dir}}

\item{ppps}{numeric: vector of PPP years.}

\item{...}{
  Arguments passed on to \code{\link{get_pip_releases}}
  \describe{
    \item{\code{owner}}{character: owner of repo}
    \item{\code{repo}}{character: repository name}
    \item{\code{file_path}}{character: file or folder path}
    \item{\code{branch}}{character: branch where the file or folder is}
    \item{\code{creds}}{list. Basically, it is \code{get_github_creds()}}
  }}

\item{confirm_remove}{logical: whether to ask for confirmation before removing folders in \code{remove_pip_release()}. Defaults to \code{getOption("pipfun.confirm_remove")}}
}
\value{
invisible TRUE if everything went fine

invisible
}
\description{
CAUTION: Use this functions with care.
}
\examples{
\dontrun{
new_pip_release()
}
}
