% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/working_release.R
\name{get_wrk_release}
\alias{get_wrk_release}
\title{Retrieve and assign the active working release}
\usage{
get_wrk_release(name = "wrk_release", verbose = getOption("pipfun.verbose"))
}
\arguments{
\item{name}{character: name to assign the working release object to in
the calling environment. Defaults to \code{"wrk_release"}.}

\item{verbose}{logical: whether to print a short confirmation message
showing the active release.}
}
\value{
Invisibly returns \code{NULL}. The primary effect is that a variable
called \code{name} is assigned in the calling frame containing the working
release list (with \code{release}, \code{identity}, and \code{ppp}).
}
\description{
Utility for functions that need access to the currently configured
working release. The function fetches the \code{wrk_release} object from
the package \code{.pipenv} environment and assigns it into the caller's
frame under \code{name} (default: \code{wrk_release}). If no working release
is set, the function aborts with an informative message.
}
\examples{

\\dontrun{
hello <- function() {
  get_wrk_release()
  invisible(wrk_release)
}
setup_working_release()
print(hello())
}

}
