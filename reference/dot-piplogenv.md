# Logging environment

This environment stores all active log files used internally by the
package. Each log is stored as a named object within this environment.

## Usage

``` r
.piplogenv
```

## Format

An object of class `environment` of length 1.
