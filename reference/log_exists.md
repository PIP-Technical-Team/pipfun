# Check whether a log exists

Check whether a log exists

## Usage

``` r
log_exists(name = getOption("pipfun.log.default"))
```

## Arguments

- name:

  Name of the log (default: `pipfun.log.default`)

## Value

Logical. `TRUE` if log exists in env .piplogenv
