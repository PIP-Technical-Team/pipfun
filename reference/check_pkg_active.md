# Check if a package is loaded or attached

Check if a package is loaded or attached

## Usage

``` r
check_pkg_active(pkg)
```

## Arguments

- pkg:

  character: Name of the package

## Value

invisible(TRUE)

## Examples

``` r
if (FALSE) { # \dontrun{
check_pkg_active
} # }
```
